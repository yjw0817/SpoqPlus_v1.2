# InsightFace Requirements for SPOQ Plus
# Windows 환경용 요구사항
# 
# 설치 방법:
# 1. Python 3.7-3.10 권장 (3.11은 일부 패키지 호환성 문제 가능)
# 2. pip install -r requirements_insightface.txt
#
# GPU 사용시:
# - CUDA 11.8 이상 설치
# - onnxruntime 대신 onnxruntime-gpu 설치

# 기본 패키지
numpy==1.23.5
opencv-python==4.8.1.78
pillow==10.1.0

# ONNX Runtime (CPU 버전)
onnxruntime==1.16.3
# GPU 사용시 위 줄을 주석처리하고 아래 주석 해제
# onnxruntime-gpu==1.16.3

# InsightFace
insightface==0.7.3

# 웹 서버
flask==3.0.0
flask-cors==4.0.0

# 데이터베이스
mysql-connector-python==8.2.0
pyodbc==4.0.39  # MSSQL 지원

# 유틸리티
python-dotenv==1.0.0
scikit-learn==1.3.2

# 선택사항 (시각화, 디버깅용)
# matplotlib==3.8.2
# jupyter==1.0.0